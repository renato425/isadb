// const stringBasica = '$createCommand[slash;name]'
// const r = stringBasica.split('$createCommand').length - 1,
// inside = stringBasica.split('$createCommand')[r]
// console.log(inside)

module.exports = require('./src/main')

// const stringBasica = '$createCommand[slash;name]',
// r = stringBasica.split('$createCommand').length - 1,
// inside = stringBasica.split('$createCommand')[r]
// console.log(inside.replace('[', '').replace(']', '').split(';'))